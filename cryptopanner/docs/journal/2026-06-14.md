# 2026-06-14

Largest single day of the project: pinned the implicit assumptions, drafted the walking-skeleton plan, and implemented the entire skeleton end-to-end across all 7 modules. Then went beyond the skeleton: multi-stream, dual-socket routing, all 7 per-symbol streams, `!forceOrder@arr` broadcast, and REST polling.

## Shipped — pre-skeleton
- `383083f` pinned 17 implicit assumptions: 10 HIGH + 7 MEDIUM (UTF-8, JSONL terminators, zstd params, SHA256 sidecar, timeouts, bearer tokens, WS/REST, seq-ID width, file perms, Linux reqs)
- `ca5488d` walking-skeleton implementation plan: `docs/superpowers/plans/2026-06-14-walking-skeleton.md` (14 tasks, TDD, tracer-bullet validation)
- `11bcb6c` bootstrap Maven wrapper

## Shipped — walking skeleton (Tasks 0–14)
- `b9c1130` common — UTC-canonicalized paths
- `638f0d0` common — sha256sum-style sidecar reader/writer
- `ba4ac72` common — minimal YAML config
- `2914f89` mock — Python websockets server replaying btcusdt@trade fixture *(superseded same day — see `2a66768`)*
- `2d5a591` collector — minimal WS client with SUBSCRIBE/ACK gate
- `16ec601` collector — minute-segment writer with zstd + sha256 sidecar
- `f30d2ba` collector — main entry point
- `c23de81` sealer — hourly merge of minute-segment files
- `1f0573a` sealer — skeleton manifest writer (11 mandatory fields)
- `0f0f173` sealer — main entry point with `--date`/`--hour` flags
- `0a78cc9` uploader — S3 upload with manifest-last ordering
- `8d5287f` uploader — main entry point
- `b90a4c4` verify — `verify` subcommand with SHA-256 round-trip check
- `0accdf7` collector — fix: tolerate whitespace in Binance ACK JSON
- `296a222` end-to-end smoke test wiring all four components — **skeleton green (289 frames)**

## Shipped — beyond the skeleton
- `b2e77ac` switched to Java 26 + bumped Spotless / google-java-format
- `2a66768` replaced Python WS mock with a Java module (single-language stack)
- `fb41467` pinned `/stream` endpoint; noted Binance schema extensibility
- `ea65de0` collector — multi-stream subscriptions with wrapper-based routing
- `0ce6862` collector — reconnect with exponential backoff + jitter
- `3d897c3` pinned dual-socket WS routing (`/public` + `/market`) — Binance May 2026 change
- `5db16a6` common — `StreamRouting` (Binance dual-socket mapping, §7.a / §8.a)
- `897c622` collector — dual-socket WS routing (`/public` + `/market`)
- `ecdb81e` sealer + uploader + verify — generalized to multi-stream
- `18cb1a2` dev — exercise all 7 per-symbol streams in mock fixture
- `493a90f` `!forceOrder@arr` all-symbol broadcast support — completes §7.a (8 WS streams e2e)
- `097c159` collector — REST polling with §8.d wrapper envelope — completes §8.d (`RestPoller`, `PollerScheduler`, `MockRestServer`, config + YAML, 9 streams e2e)

## Context / decisions
- **TDD discipline held through all 14 plan tasks.** Each task = failing test → minimal impl → commit. No skipped test phase.
- **Java mock replaces Python mock the same day.** Single-language stack avoids Python tooling on developer machines and CI.
- **Java 26 bump was opportunistic** — discovered after skeleton was green that it builds cleanly; Spotless / google-java-format catch up at the same time.
- **Dual-socket routing was unplanned.** Discovered during multi-stream validation that Binance had split `/public` (trade, depth, bookTicker) vs `/market` (aggTrade, kline, ticker, markPrice, forceOrder) in May 2026. Pinned in spec (`3d897c3`), then in code (`5db16a6`, `897c622`). 809 trade + 288 aggTrade validated.
- **9-stream e2e validation:** 536 trade + 44 aggTrade + 7 per-symbol streams + forceOrder + REST polling, all with `ERRORS=0`.
- **§8.d REST polling completed today** so the open-interest-like streams that don't have a WS form are covered before the skeleton-deferred list is reconsidered.

## Next
- Gap detection — sequence-ID continuity validation for `trade` / `aggTrade`.
- Continue against the skeleton-plan's deferred list: server-event-time bucketing, seal-grace windows, REST backfill, hot-swap, Node Agent, Monitor, chaos scenarios.
