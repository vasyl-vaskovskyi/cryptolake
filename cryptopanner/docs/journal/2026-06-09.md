# 2026-06-09

Designed the unified make-before-break protocol that covers both operator-driven JAR deploys and daily WS rotations, then wired it back into the master spec.

## Shipped
- `77124fe` markdown rendering fix (master spec sections 2,4,6,7,9,10,13,15 — lettered lists → bullets so nested items render as sub-lists, not parent-folds)
- `06dc2bd` design doc: `docs/superpowers/specs/2026-06-09-collector-hot-swap-and-ws-rotation-design.md` — systemd `@a`/`@b` templates, E-field bucketing, shared `EquivalenceChecker`
- `df33e68` §5 reviewed; hot-swap wired into master spec
- `01a188e` §6 reviewed; node anatomy reflects slot templating, concurrent-JVM resources, per-slot heartbeats/logs, `/data` separate-volume recommendation
- `966005c` §7 reviewed; data inventory tightened
- `60038be` §8 reviewed; ingest semantics tightened (stream bucketing field, SUBSCRIBE-WS form)
- `f81957d` scheduling rules for sealer (120s grace), deploy, rotation (forbidden zones HH:50→HH:10/HH:15)
- `0c6a6d5` §9 reviewed (durability fsync order, staging removal, `.fs-heavy.lock`)
- `98a9ea8` §10 reviewed; file format + manifest tightened

## Context / decisions
- **Unified protocol for two operations that look different.** JAR deploys (process swap) and daily WS rotations (in-process socket swap) both share the make-before-break invariant. A single shared mechanism (`EquivalenceChecker` + `OverlapMerger`) services both — Variant A and Variant B in the design doc.
- **Removed the standalone daily-rotation §5** from the master spec; it was misplaced and is now folded into §5 (deployment) + the design doc.
- **Forbidden zones HH:50→HH:10/HH:15** guarantee Sealer's 120 s grace window never collides with rotation cutover or deploy promote.
- **E-field bucketing** (server event time) replaces local receive time as the minute-bucket key. Documented in §8; *not yet implemented in the collector*.

## Next
- Continue per-section review: §11 (observability), §12 (failure model), §13 (alerting), §14 (testing), §15 (configuration), §16 (open questions).
