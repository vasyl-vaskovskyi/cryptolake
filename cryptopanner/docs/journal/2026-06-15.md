# 2026-06-15

Knocked three items off the skeleton-plan's deferred list: sequence-ID gap detection, REST backfill for the gap-fillable streams, and opened the chaos framework with the first scenario.

## Shipped
- `37baa08` sealer — sequence-ID continuity + gap envelopes for `trade` / `aggTrade`
- `ed8f775` sealer — REST backfill for `trade` / `aggTrade` gaps (`RestBackfiller`, `BackfillAttempt`, `HourMerger` rewrite, `SkeletonConfig.restApiKey`)
- `87e6883` chaos — framework + scenario 01 (collector active crash)

## Context / decisions
- **Gap detection first, backfill second.** Detection without backfill leaves the archive honest (gaps are visible); backfill without detection would silently paper over real problems. The order matters.
- **`trade` / `aggTrade` only for now.** They have well-defined sequence IDs and matching REST endpoints. `depth` (`U`/`u`/`pu`-chain) and the others need separate validators and are deferred.
- **Chaos framework opens with scenario 01** (collector active-slot crash) as the canonical shape: isolated `cryptopanner-chaos-NN` compose project, fault injection, assert `cryptopanner-verify ERRORS=0` and expected gap annotations in the manifest. The other 17 scenarios in §14.e use the same shape.

## In-flight (end of day)
- The REST backfill work landed (commits in tree), but the `.remember/now.md` buffer flagged earlier in the day still references the in-progress version of `HourMerger` + `SkeletonConfig.restApiKey` from before `ed8f775` was committed. That's stale; the commits are final.

## Next
- Sequence-ID validation for the remaining streams (depth `U`/`u`/`pu`-chain, kline, etc.).
- Chaos scenarios 02–18 against the framework opened today.
- Plan file for hot-swap (Variant A) + WS rotation (Variant B) — too large and too interlocked to wing.
