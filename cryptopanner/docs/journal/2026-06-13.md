# 2026-06-13

Pre-implementation cleanup: pinned the timezone invariant, swapped build systems.

## Shipped
- `7a4b118` switched build system from Gradle to Maven
- `b1785cb` pinned UTC as the system-wide timezone (master spec §6.a/§8.e/§10.c/§15)

## Context / decisions
- **Maven over Gradle.** Aligns with CryptoLake v1's parent convention. Spotless integration via `verify` phase, `appassembler-maven-plugin` for the `installDist`-equivalent layout. Switch was cheap because the scaffolding was 1-day-old.
- **UTC everywhere.** Eliminates DST/locale ambiguity in path derivation, file naming, S3 keys, and manifest timestamps. Local time is forbidden in load-bearing code paths.

## Next
- Draft the walking-skeleton implementation plan (TDD task list, all 7 modules).
- Pin remaining implicit assumptions (UTF-8, JSONL terminators, zstd params, SHA256 sidecar format, timeouts, bearer tokens, WS/REST, seq-ID width, file perms, Linux reqs).
