from __future__ import annotations

import click


@click.command(help="Verify CryptoLake data captures.")
def cli() -> None:
    """Placeholder verification command until later chunks add full checks."""
    click.echo("Verification commands will be implemented in a later chunk.")
